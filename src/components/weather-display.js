/*
 *
 *   TODO: display the weather when the component receives props
 *
 */

import React from 'react';

const WeatherDisplay = props => {
    return <div>{props.weather.title}</div>;
};

export default WeatherDisplay;
